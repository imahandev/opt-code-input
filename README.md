# فرم ورود کد ۴ رقمی — پروژهٔ HTML + Tailwind CSS + JavaScript

یک پروژهٔ ساده و سبک برای گرفتن **کد تأیید ۴ رقمی (OTP / PIN)** با HTML، Tailwind CSS و JavaScript. شامل اعتبارسنجی، ارسال درخواست (نمونه‌ی `response` از API) و تجربۀ کاربری دوستانه — آمادهٔ قرار گرفتن در گیت‌هاب.

---

## 🔎 توضیحات کلی
این پروژه یک فرم ورود کد ۴ رقمی پیاده‌سازی‌شده با:
- ساختار سادهٔ HTML
- استایل با Tailwind CSS (از CDN برای ساده‌سازی)
- منطق و اعتبارسنجی با JavaScript
- نمایش پاسخ (response) شبیه‌سازی‌شده از یک API

هدف: نمایش UX مناسب برای وارد کردن کد، پشتیبانی از حذف/جابه‌جایی خودکار فیلدها و ارسال کد به سرور.

---

## ✨ امکانات
- ۴ فیلد جدا برای هر رقم  
- حرکت خودکار به فیلد بعدی پس از وارد کردن رقم  
- پشتیبانی از Backspace برای برگشت  
- اعتبارسنجی اینکه همهٔ فیلدها پر شده باشند  
- شبیه‌سازی ارسال به API و نمایش `response` (موفق یا خطا)  
- ظاهر واکنش‌گرا و ساده با Tailwind  


